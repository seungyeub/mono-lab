# Phase 6 PRD: 결함 정리·기능 연결·측정 인프라 신뢰성

> **작성일:** 2026-09-08
> **Title:** Phase 6 — 0.4.0 배포 이후의 결함 수정, 미연결 기능 완성, 측정·배포 인프라 신뢰성 확보
> **Description:** Phase 5(P0~P3 16항목)가 0.4.0으로 배포된 뒤 남은 것을 세 갈래로 묶는다. 실측으로 확인한 화면 결함 1건, 코드는 있으나 연결되지 않은 기능(문의 메일·분석·접근성) 3건, 수치를 믿을 수 없는 측정·배포 인프라 3건, 그리고 결정만 남은 항목 2건이다.
> **전제 버전:** v0.4.0 (2026-09-07 릴리스). develop에는 0.5.0 대상(OG 전용 이미지·Projects 이름 통일·등장 효과 프리셋, PR #77)이 이미 쌓여 있다.
> **진행 원칙:** P1·P2는 항목마다 배경과 선택지를 설명한 뒤 확인을 받고 착수한다(2026-09-08 요청). 코드 변경은 항목별 커밋, PR은 묶어서 하나. 원격에 올리는 동작은 매번 확인받는다.

---

## 1. 목표 (Goals)

1. **눈에 보이는 결함을 0으로** — 세로로 긴 화면에서 에필로그 문구가 끝까지 밝아지지 않는 현상을 고친다.
2. **만들어 두고 연결하지 않은 기능을 완성** — 방문 분석, 접근성 기준 충족. (문의 폼 메일 발송은 조사 결과 결함으로 드러나 P0로 옮겼다.)
3. **측정과 배포를 믿을 수 있게** — CI Lighthouse 수치가 로컬과 0.45점 차이 나는 원인을 볼 수 있게 하고, 릴리스 도구의 운영 구멍을 메우고, 타입 오류가 배포되는 경로를 막을지 결정한다.

---

## 2. 우선순위별 작업 목록 (Prioritized Backlog)

### 🔴 P0 — 결함 수정

- **P6-1. 에필로그 문구가 세로로 긴 화면에서 끝까지 밝아지지 않음** ✅ 완료 (2026-09-08)
  `ScrollRevealText`는 스크롤 진행도를 "문단 시작이 화면 높이 90% 지점 → 문단 끝이 55% 지점"으로 잰다(`offset: ['start 0.9', 'end 0.55']`). 화면이 세로로 길면 페이지를 끝까지 내려도 문단 끝이 55% 지점까지 올라오지 못해 진행도가 1에 닿지 않고, 뒤쪽 단어가 어두운 채로 남는다. 아래에 Footer가 있어 스크롤이 더 남지 않는 것도 겹친다.
  - **실측(2026-09-08, 끝까지 스크롤 후 30개 단어의 불투명도)**: 1440×900·1440×1200·412×915는 30/30 완료. **1440×1600은 29/30(마지막 단어 0.43), 1280×2000은 22/30(8개가 0.12로 시작값 그대로).**
  - **방향 두 가지**: ⓐ 진행도 기준을 화면 높이에 덜 의존하게 바꾼다(예: `end` 기준을 화면 하단 쪽으로). ⓑ 단어 구간을 진행도 0~0.8 안에 압축해 1에 닿지 않아도 마지막 단어가 끝나게 한다. 착수 시 두 안을 로컬에서 나란히 비교한다.
  - **검증**: 위 다섯 해상도 + 1440×2400에서 30/30 확인. 짧은 화면에서 너무 일찍 끝나 효과가 사라지지 않는지도 함께 본다.
  - **결과**: ⓑ를 화면에 맞춰 자동으로 하는 방식으로 구현했다. 첫 렌더 때 "끝까지 내렸을 때 도달할 수 있는 최대 진행도"를 재 두고(창 크기·문단 높이 변화 시 재측정) 단어 구간을 그 안에 맞춘다. 짧은 화면은 최대치가 1이라 원래와 동일하다. ⓐ는 어떤 고정값을 골라도 더 긴 화면에서 다시 실패해 택하지 않았다. 여섯 해상도(900·1200·1600·2000·2400·모바일 915) 모두 30/30, 짧은 화면의 중간 지점에서는 20/30만 밝아 순차 효과 유지 확인. 테스트 변경 없음(단어별 `useTransform` 호출 수 유지).

- **P6-2. 문의 폼이 메일을 보내지 않으면서 "보냈다"고 표시함** ✅ 완료 (2026-09-08) — **P1에서 P0로 상향**
  계획 단계에는 "`actions.ts`의 Resend 액션이 미연결"로 적었으나 조사 결과 실상은 달랐다. 폼은 브라우저에서 EmailJS로 보내도록 돼 있었고, 키가 없으면 콘솔에만 남기고 성공으로 처리하는 개발용 분기가 있었다. **프로덕션에 EmailJS 키가 등록돼 있지 않아 그 분기를 타고 있었다** — 실제 제출로 확인(EmailJS 요청 0건, 콘솔 "keys not set", 화면 "Message sent successfully"). 방문자는 연락이 닿았다고 믿지만 메일은 어디에도 가지 않았다.
  - **결정(2026-09-08)**: Resend 서버 액션으로 일원화(키가 서버에만 있음. EmailJS는 공개 키가 번들에 실려 누구나 발송 요청을 쏠 수 있다). 수신·공개 이메일은 `superior051@gmail.com`. 설정이 없으면 성공 가장 대신 오류와 직접 연락 안내. 서버 재검증 + 허니팟. `@emailjs/browser` 제거.
  - **구현**: `src/lib/contactSchema.ts`(폼·서버 공용 zod 규칙, 허니팟 `company`), `src/lib/actions.ts`(재검증 → 허니팟이면 성공인 척 → 설정 없으면 `unavailable` → Resend `replyTo`=방문자, HTML 이스케이프, `{error}`·예외 모두 `failed`), `ContactForm`(서버 액션 호출, 오류 시 `mailto:` 안내, 화면 밖 허니팟 `tabIndex=-1`), `siteConfig.CONTACT_PUBLIC_EMAIL` 단일 소스, `actions.test.ts` 7건.
  - **Resend 제약**: 도메인 인증 없이는 발신이 `onboarding@resend.dev`, 수신은 계정 이메일뿐이다. 수신자가 본인 한 명이라 문제없고, 방문자 주소는 `replyTo`로 싣는다. 무료 한도 월 3,000통·일 100통. `seungyeub.vercel.app`은 DNS를 만질 수 없어 도메인 인증은 개인 도메인을 붙일 때 가능.
  - **환경 변수**: Vercel Production에 `RESEND_API_KEY`(Secret)·`CONTACT_EMAIL`(Config) 등록 완료(2026-09-08, 사용자). 값은 저장소·대화에 두지 않는다.
  - **검증**: 게이트·빌드 통과. 키 없는 로컬에서 제출 → "sent successfully" 미표시, "Sending is unavailable… superior051@gmail.com" 안내와 `mailto:` 링크 표시, 외부 요청 0건, 서버 로그에 설정 누락 경고 1건. 허니팟은 화면 밖·탭 순서 제외 확인. **실제 발송은 배포 후 프로덕션에서 1회 확인 필요.**

### 🟡 P1 — 기능 연결 (항목마다 설명 후 착수)

- **P6-3. 방문 분석 연동** ✅ 완료 (2026-09-09) — 측정 ID `G-SJXH41DRD4` Vercel 등록 완료, Speed Insights는 코드 배포 시 자동 활성
  루트 `docs/PRD.md`가 Phase 6로 잡아 둔 항목.
  - **결정(2026-09-09)**: Vercel Web Analytics를 권했으나 **GA4로 확정**(Search Console 연동, 장기 보관, 풍부한 리포트). 문의 제출을 커스텀 이벤트로 기록. Speed Insights(실사용자 Core Web Vitals)도 함께 켠다 — P6-6에서 CI Lighthouse와 비교할 기준이 된다.
  - **구현**: `@next/third-parties`(Next와 같은 16.2.0)의 `GoogleAnalytics`를 루트 레이아웃 `</body>` 뒤에 두되 `NEXT_PUBLIC_GA_MEASUREMENT_ID`가 있을 때만 싣는다(로컬·프리뷰 데이터 오염 방지, 등록 전 배포도 안전). `@vercel/speed-insights/next`의 `SpeedInsights`를 body 안에. `ContactForm`은 제출 성공 시 `sendGAEvent('event','contact_submit',{form:'contact'})` — 입력값은 보내지 않는다. ID는 `siteConfig.GA_MEASUREMENT_ID` 한 곳에서 읽는다.
  - **검증**: ID 없는 빌드 — gtag 스크립트 0개, Speed Insights 스크립트는 클라이언트 주입 확인. 임시 ID(`G-TEST0000`) 빌드 — SSR에 gtag 태그 1개, 허니팟 경로로 성공 분기를 태워 `dataLayer`에 `contact_submit` 이벤트 적재 확인(외부 요청은 차단). 게이트·빌드 통과.
  - **사용자 작업(완료 2026-09-09)**: GA4 속성 생성(업종: 직업/구직, 목표: 트래픽 파악·리드 생성) → 웹 스트림 → 측정 ID `G-SJXH41DRD4`(공개 값) → Vercel Production에 `NEXT_PUBLIC_GA_MEASUREMENT_ID`(Config) 등록. `NEXT_PUBLIC_`은 빌드 시 박히므로 다음 배포부터 적용. Speed Insights는 대시보드에 별도 Enable 버튼이 없고 패키지가 포함된 배포가 나가면 수집이 시작된다. **배포 후 GA4 실시간 보고서와 Speed Insights 탭에서 수집 확인 필요.**
  - **남은 결정**: GA4는 쿠키를 쓴다. 개인정보 처리방침 문구(수집 항목·목적·GA 사용)를 어디에 둘지 — 푸터 한 줄 링크 + 짧은 페이지를 권한다. P6-4(접근성)와 묶어 다룰 수 있다.
- **P6-4. 접근성 점검·개선** ✅ 완료 (2026-09-09) — 자동 감사 실패는 의도된 연출 2종만 남음
  - **점검 항목**: 키보드만으로 전 페이지 이동, 포커스 표시, 색 대비(`white/40` 계열 보조 텍스트가 기준 미달일 가능성), 스크린리더 레이블(아이콘·장식 요소), 애니메이션 감소 설정(`prefers-reduced-motion`) 존중 여부.
  - **주의**: 전역 `cursor: none`과 커스텀 커서가 키보드·보조기기 사용자에게 어떤 영향을 주는지 먼저 본다.
  - **확인된 결함(2026-09-09)**: `/contact`·`/work`·`/resume`에 `<main>`이 두 개다. 루트 레이아웃이 `<main>`으로 감싸는데 세 페이지가 또 `<main>`을 렌더한다(홈은 1개). 랜드마크 중복이라 스크린리더가 본문을 두 번 안내한다. 페이지 쪽을 `<div>`나 `<section>`으로 바꾸면 된다.
  - **조사(2026-09-09)**: Lighthouse 접근성 0.93~0.96. 실패는 color-contrast(전 페이지, 홈 71개 중 대부분 에필로그 문구의 시작 밝기 0.12), target-size(상세 캐러셀 점 6px·일시정지 9px), heading-order(홈 h1→h6, /work h1→h3). 감소 모션 미대응, 전역 `cursor: none !important`, 건너뛰기 링크 없음. 키보드 탭 순서와 포커스 표시는 정상(25/25).
  - **B 구조(완료)**: 페이지 `<main>`→`<div>`(레이아웃의 `<main id="main">` 하나만), 섹션 라벨 `<h6>`→`<p>`, /work 카드 `<h3>`→`<h2>`, 건너뛰기 링크, RollingText 글자 레이어 `aria-hidden`. heading-order 해소, 모든 페이지 `<main>` 1개.
  - **A 감소 모션(완료)**: `MotionProvider`(`MotionConfig reducedMotion="user"`)로 framer-motion 전체가 OS 설정을 따른다. 그 밖의 것은 개별 분기 — 마퀴는 CSS `animation: none`, Lenis 스무스 스크롤 생략, Hero 3D 캔버스 대신 정적 플레이스홀더, 에필로그 문구는 처음부터 밝게, 캐러셀 자동 넘김 꺼짐(버튼으로 켤 수는 있음). 실측: reduce 모드에서 마퀴 none·캔버스 없음·lenis 없음·어두운 단어 0/30·자동 넘김 정지.
  - **C 캐러셀(완료)**: 자동 재생과 일시정지 버튼은 유지(WCAG 2.2.2 — 5초 이상 자동 움직임에는 멈출 수단 필요). 글리프 크기는 그대로, 색을 white/50→80(버튼)·white/30→50(점)으로 올려 보이게 하고, 클릭 영역만 24×24로 넓혔다. target-size 해소, 상세 0.93→0.96.
  - **E 커서(완료)**: `cursor: none`을 `@media (pointer: fine) and (prefers-reduced-motion: no-preference)` 안으로. 그 밖에서는 `.cursor-none`도 무시. CustomCursor 표시 조건도 같은 미디어 변형 하나로(`motion-reduce:hidden`을 따로 두면 임의 미디어 변형의 flex에 밀려 적용되지 않았다 — 실측). reduce 모드에서 body cursor auto·래퍼 display none 확인.
  - **D 대비(완료)**: 실패는 세 부류였다. ① `text-white/40` 라벨·보조 텍스트 23곳(3.77:1) — 45%는 4.48로 미달, **50%(5.29:1)로 일괄 교체**. 캡쳐로 확인했을 때 눈으로 거의 구분되지 않는 변화. ② 에필로그 문구의 시작 밝기 0.12(1.32:1) — 스크롤로 밝아지는 연출의 시작 상태라 **유지**. 동작 줄이기 사용자는 A에서 처음부터 밝게 처리된다. ③ 푸터 워터마크 "SEUNGYEUB ©2026"(1.76:1) — 장식용 대형 글자라 **유지**. 밝히면 워터마크가 아니게 된다.
  - **결과**: 5개 페이지 접근성 0.95~0.96. 남은 자동 감사 실패는 ②③뿐이고 그 외 항목(heading-order·target-size·랜드마크)은 0. 개인정보 처리방침(P6-3에서 미결)은 별도 항목으로 다룬다.
  - **도구**: Lighthouse 접근성 점수(현재 CI에서 측정 중), axe 기반 자동 검사, 수동 키보드 탐색.

- **P6-10. Contact 페이지 레이아웃 정리** ✅ 완료 (2026-09-09)
  P6-2 작업 중 캡쳐로 확인한 문제: 데스크톱에서 제목 아래 약 250px와 오른쪽 절반이 비어 있고(폼이 왼쪽 절반만 차지), 입력란에 라벨 없이 흐린 placeholder만 있으며, 제출 버튼만 각진 사각형(다른 페이지는 알약형), Quick Links·Networks가 헤더·연락처 열·푸터에 세 번 반복되고, 안내 문장이 없다.
  - **방향(합의)**: 헤더는 제목 + 한 줄 안내로 줄이고 연락처를 폼 옆(7:5)으로 내린다. 입력란에 `text-label` 라벨을 붙이고 placeholder는 예시 값으로. 버튼은 `RollingButton` 알약형으로 통일. 연락처 열에서 SNS 목록은 빼고 이메일·위치만(SNS는 푸터가 맡음). 모바일은 정보 → 폼 순서 유지.
  - **범위 주의**: Contact는 VRT 대상이 아니라 스냅샷 재촬영 없음. P6-2(결함)와는 커밋을 분리한다.
  - **2차 조정(2026-09-09, 참고 이미지 상의)**: 제목을 "Let's Talk About Your Project©"로(페이지 목적을 제목이 말하게). 정보 열은 Email·Based in·Networks·Response 4항목을 구분선 목록으로 — Networks를 다시 넣은 이유는 푸터(사이트 내비게이션)와 역할이 다른 "연락 채널"이기 때문. 전화·주소는 개인 사이트라 넣지 않는다. 폼 위에 "Send a message" 라벨. **`/work`·`/resume`·`/contact` 본문에 `site-container` 적용** — 헤더·푸터·상세만 1480px 컨테이너를 쓰고 세 페이지는 끝까지 늘어나던 불일치.
  - **3차 조정(2026-09-09, 화면 확인 후)**: 헤더 안내 문장 제거. 제목은 채용 위주로 "Let's Talk About Your Team©". 연락처 열은 320px로 오른쪽 끝에, 폼은 최대 896px. Response 행 제거, 이메일은 다른 글자와 같은 14px 회색, 구역 라벨(Send a Message·Contact)은 헤더 Quick Links와 같은 `font-semibold text-white`, 항목 라벨은 흰색. 연락처 행과 입력란 모두 hover(입력란은 focus 포함) 시 아래 선이 왼쪽에서 오른쪽으로 흰색으로 채워지고, 링크 글자는 `RollingText`로 굴러간다 — 글자별 지연이 쌓여 느려서 `stagger` 옵션(기본 25ms 유지)을 추가해 여기서만 0ms. 버튼은 폼 가운데, 홈 CONTACT와 같은 크기(`md:text-[23px]`), 입력란과 64px 간격. 모바일 연락처→폼 간격 80px.
- **P6-11. Contact 페이지에 Quick Answers(FAQ) 구역 추가** (2026-09-09 등재, 보류)
  참고 이미지의 하단 구역. 문의 전에 궁금한 것(어떻게 시작하나, 답장은 언제쯤, 원격 협업 가능 여부 등)을 미리 답해 두면 문의 문턱이 낮아지고 FAQPage 스키마도 얻는다. 홈 FAQ는 기술 질문이라 그대로 쓰지 않고 문의용 질문 3~4개를 새로 쓴다. **질문·답변 초안을 만들어 확인받은 뒤 착수.**
- **P6-12. 개인정보 처리방침 페이지** (2026-09-09 등재, 착수 전 상의)
  P6-2로 문의 폼이 이름·이메일·메시지를 실제로 수집하고, P6-3으로 GA4가 쿠키로 방문을 기록한다. 개인정보 보호법은 수집 주체에게 목적·보관 기간·제3자 제공을 공개하도록 요구하고, **Google Analytics 이용약관은 GA를 쓰는 사이트에 처리방침 게시와 쿠키·식별자 사용 고지를 요구**한다(법과 별개의 약관상 의무).
  - **범위 안**: `/privacy` 짧은 페이지(수집 항목·목적·보관·제3자(Google, Resend)·거부 방법·연락처) + 푸터 링크 한 줄. 채용 담당자가 보는 사이트에서 신뢰 요소이기도 하다.
  - **착수 전 확인할 것**: 문구 초안, 문의 메일 보관 기간, GA 데이터 보관 설정.

### 🟡 P2 — 성능·인프라 (항목마다 설명 후 착수)

- **P6-5. 초기 JS 번들 감량**
  P3-11에서 관측 LCP는 195ms까지 내렸지만 Lighthouse 보고값은 4.3s다. 시뮬레이션이 관측 LCP 이전에 시작된 요청(초기 JS 약 290KB + Pretendard 서브셋 13개 약 330KB)을 전부 의존으로 계산하기 때문이다. 보고값을 더 내리려면 이 바이트를 줄여야 한다. 실제 체감보다 지표 개선 성격임을 전제로 한다.
  - **후보**: framer-motion 사용 범위 점검, 홈에서만 쓰는 모듈의 지연 로딩, 폰트 서브셋 수 확인.
- **P6-6. CI Lighthouse 신뢰성** ✅ 1차 완료 (2026-09-09) — 리포트 보관·댓글 수정·3회 중앙값. 프리뷰 URL 측정은 보류
  - **현상**: CI 성능 점수 0.3, 로컬 0.75. 리포트 아티팩트를 저장하지 않아 러너 CPU 지표(`benchmarkIndex`)나 관측값을 볼 수 없다. "PR 코멘트" 단계는 `Manifest not found`로 매번 실패해 코멘트가 달리지 않는다.
  - **방향**: ① 리포트 JSON을 아티팩트로 저장 ② 코멘트 단계 수정 ③ 러너 빌드 대신 Vercel 프리뷰 URL을 측정하는 방식 검토 ④ 그 뒤 `.lighthouserc.cjs` 임계값(`warn 0.7`) 상향 여부 결정.
  - **원인 확정**: `Manifest not found`는 업로드 방식이 `temporary-public-storage`라 `manifest.json`이 생성된 적이 없어서였다(`filesystem` 타깃만 쓴다). 점수 격차는 2코어 러너에서 빌드·서버·크롬을 함께 돌린 실측 위에 시뮬레이션이 4배를 다시 곱하는 구조 + 1회 실행 편차. 로컬 3회 실행에서도 73·83·81로 갈렸다.
  - **구현**: `.lighthouserc.cjs` — `numberOfRuns: 3`, `assert.aggregationMethod: 'median'`, `upload.target: 'filesystem'`(`.lighthouseci/`, gitignore됨). `lighthouse.yml` — 리포트 디렉터리를 아티팩트(`lighthouse-reports-<PR>-<attempt>`, 14일)로 업로드, 댓글에 시뮬레이션·관측 LCP/FCP, TBT, 러너 `benchmarkIndex`(로컬 M-시리즈 약 3000) 추가, 표식으로 기존 댓글을 찾아 갱신(푸시마다 쌓이지 않음). 로컬에서 새 설정으로 실행해 manifest·대표 실행·댓글 본문 생성을 확인했다.
  - **보류**: Vercel 프리뷰 URL 측정. 프로젝트에 Vercel Authentication이 켜져 있어(커스텀 도메인 외 전부) 프리뷰가 로그인 화면을 낸다. 공개로 바꾸는 것은 보안 설정 변경이라 리포트로 러너 문제를 확인한 뒤 다시 결정(2026-09-09 합의). 임계값(`warn 0.7`)도 그때.
- **P6-7. release-please 수동 실행 트리거** ✅ 완료 (2026-09-09)
  0.4.0 릴리스 때 `.prettierignore` 수정 뒤 릴리스 PR을 다시 만들려 했으나, 워크플로가 master push에만 반응해 방법이 없었다. `workflow_dispatch`를 추가한다. 한 줄 변경이지만 master로 가야 효과가 있으므로 다음 릴리스 PR에 실어 보낸다.
  - **구현**: `on.workflow_dispatch` 추가. `target-branch: master`가 고정돼 있어 어느 브랜치에서 실행해도 master 기준으로 릴리스 PR을 만들거나 갱신한다. 사용법: Actions → Release Please → Run workflow. 효과는 이 변경이 master에 들어간 뒤부터.

### 🟢 P3 — 결정 필요

- **P6-8. Resume PDF 다운로드 추가**
  웹 페이지(`/resume`)는 이미 있고 경력·기술 데이터를 재사용한다. **PDF도 함께 제공하기로 방향을 정했다(2026-09-08).** 남은 결정은 PDF를 만드는 방식이다.
  - ⓐ 직접 만든 PDF를 `public/`에 두고 링크 — 단순하지만 페이지 내용과 어긋날 수 있다.
  - ⓑ 페이지에서 자동 생성(인쇄용 스타일 + 브라우저 인쇄, 또는 빌드 시 생성) — 항상 최신이지만 레이아웃 작업이 따로 든다.
- **P6-9. 배포 파이프라인의 타입 체크 게이트** ✅ 완료 (2026-09-09) — 둘 다 적용
  `next.config.ts`의 `typescript.ignoreBuildErrors: true`와 태그 push만으로 도는 `deploy-portfolio.yml`이 겹쳐 **타입 오류가 있어도 배포된다.** PR CI는 검사를 돌리지만 태그 경로는 그 관문을 거치지 않는다. release-please가 태그를 만드는 지금은 릴리스 PR의 CI가 사실상 관문이지만, 사람이 태그를 직접 밀면 여전히 비어 있다.
  - **선택지**: `ignoreBuildErrors` 제거(숨은 타입 오류가 드러나면 한 번 정리 필요) / 배포 워크플로에 `check-types` 단계 추가 / 둘 다.
  - **결정·구현(2026-09-09)**: 둘 다. `next.config.ts`에서 `typescript.ignoreBuildErrors` 제거 — 로컬 프로덕션 빌드로 숨은 오류가 없음을 확인. `deploy-portfolio.yml`에 `pnpm install` + `turbo run check-types --filter portfolio`를 `vercel build` 앞에 추가 — 태그를 직접 밀어도 관문이 생긴다.

---

## 3. 목록에서 제외한 항목과 이유 (2026-09-08 확정)

| 후보                                                | 결론                                                                                                                                                |
| --------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------- |
| 모바일 Hero에서 h1 마지막 줄과 사원증 끈이 겹침     | **현행 유지.** 의도된 레이어 연출이라고 확인받았다. 끈이 "구축하는"과 마침표를 가로지르지만 문장은 읽힌다.                                          |
| 데스크톱 Hero 좌측 메타 첫 줄 미표시                | **오판.** 1920·1440·1280·1024·820·768 여섯 폭에서 두 줄 모두 표시되고 헤더에 가리지 않음을 실측했다. VRT 스냅샷을 축소해 읽다가 한 줄로 잘못 본 것. |
| Work 상세 실물 이미지 확보(Phase 5 Open Question 3) | **대부분 해소.** 11개 중 9개가 실제 캡쳐를 갖고 있고, 사내 화면이라 공개할 수 없는 `tmon-dashboard`·`yoga-editor`는 텍스트 카드로 두기로 확정.      |
| More Works UX(Phase 5 Open Question 4)              | **이미 해소.** PR #52에서 "다음 프로젝트" 순차 탐색으로 교체됐다. PRD의 "추천 2개 격자" 서술이 옛 상태였다.                                         |
| 등장 효과 통합 관리                                 | **완료.** PR #77에서 `src/lib/motion.ts` 프리셋 8개로 통합.                                                                                         |

---

## 4. 진행 방식과 검증

- **순서**: P6-1 → P6-2(P0로 상향) → P6-3 → P6-4 → P6-5 → P6-6 → P6-7. P6-8·P6-9는 결정이 나면 끼워 넣는다.
- **브랜치·PR**: 한 브랜치에 항목별 커밋, PR 하나. 100파일을 넘기면 분할 여부를 확인한다.
- **게이트**: `pnpm run format && pnpm exec turbo run lint check-types test --force`, `pnpm run build`. VRT는 컨테이너 재촬영만 신뢰한다.
- **릴리스**: develop에 쌓인 0.5.0 대상과 함께 배포한다. `feat`가 있어 0.5.0. develop→master는 Merge commit, 릴리스 후 master→develop 역머지를 빠뜨리지 않는다(0.3.0 때 빠뜨려 PR #72로 따로 해소했다).

---

## 5. 참고

- Phase 5 PRD: `docs/plan/2026-08-18_phase5_prd.md`
- 릴리스 자동화: `docs/plan/2026-09-03_release-automation.md`
- 루트 로드맵의 Phase 6 정의: `docs/PRD.md` ("Lighthouse 성능/접근성 최적화, SEO 및 Google Analytics 연동") — 성능·SEO는 Phase 5에서 처리됐고 접근성·분석이 이 문서의 P6-3·P6-4로 이어진다.
